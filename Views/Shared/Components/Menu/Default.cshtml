@model List<DotnetCoreIdentity.Models.MenuLinkTitles>

@foreach (var item in Model)
{
    <li class="nav-item">
        <a class="nav-link text-dark" asp-action="Index" asp-controller="@item.ControllerName">
            @item.MenuLinkTitle
        </a>
    </li>
}
